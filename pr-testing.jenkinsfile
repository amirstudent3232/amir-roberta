pipeline {
    agent any

    stages {
        stage('Unittest   ') {
            steps {
                //pip install -r requirements.txt
                sh '''
                echo "testing"
                python3 -m pytest --junitxml results.xml tests
                '''
            }
        }
        stage('Lint') {
            steps {
                sh 'echo "linting"'
            }
        }
        stage('Functional test') {
            steps {
                sh 'echo "testing"'
            }
        }
    }
}